#include <bits/stdc++.h>

int main() {
    int a;
    scanf("%d", &a);
    if (a > 0) {
        printf("positif\n");
    } else if (a < 0) {
        printf("negatif\n");
    } else {
        printf("nol\n");
    }
}